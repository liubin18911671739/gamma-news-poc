GAMMA_API_KEY=your_gamma_api_key
# Optional: enable Chinese keyword -> English keyword translation for RSS search
ZHIPUAI_API_KEY=your_zhipuai_api_key
# Optional: translation model (default: glm-4-flash)
# ZHIPU_TRANSLATE_MODEL=glm-4-flash
# Optional: translation timeout in ms (default: 5000)
# ZHIPU_TRANSLATE_TIMEOUT_MS=5000
# Optional: enrichment facts per item (default: 2)
# ENRICH_FACTS_PER_ITEM=2
# Optional: related news count per item (default: 3)
# ENRICH_RELATED_LIMIT=3
# Optional: enrichment concurrency (default: 3)
# ENRICH_CONCURRENCY=3
# Optional: article fetch timeout in ms for enrichment (default: 4500)
# ENRICH_FETCH_TIMEOUT_MS=4500
# Optional: enrichment model (default: glm-4-flash)
# ZHIPU_ENRICH_MODEL=glm-4-flash
# Optional: enrichment model timeout in ms (default: 15000)
# ZHIPU_ENRICH_TIMEOUT_MS=15000
# Optional: override RSS source
# RSS_URL=https://news.google.com/rss/search?q=artificial%20intelligence%20geopolitics%20regional%20policy&hl=en-US&gl=US&ceid=US:en
